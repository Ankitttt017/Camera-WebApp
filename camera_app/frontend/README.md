# Frontend

React UI for the Machine Event Recorder. It talks to the FastAPI helper on `127.0.0.1:8010`.

## Run

```powershell
cd camera_app\frontend
npm.cmd install
npm.cmd run dev
```

Open `http://127.0.0.1:5173`.
